$(document).ready(function(){
    // Fonction pour afficher le formulaire avec un effet de descente
   // le script est appelé lorsque la page est chargée dans le navigateur	    
function afficherFormulaire() {
        $('#modal_con').slideDown(1000);
    }

    
    afficherFormulaire();
});
